# 山寨币放量突破（独立高风险 sleeve）
#
# 10x 仅是交易所保证金杠杆。仓位按 risk_per_trade / stop_pct 计算：
# 1000 USDT × 10% / 5% = 2000 USDT 名义仓位，约占 200 USDT 保证金。
# 不允许把 1000 USDT 直接开成 10000 USDT；一次 5% 止损约损失 100 USDT（10% 本金）。

[altcoin_impulse]
enabled = true
capital_usdt = 1000.0
exchange_leverage = 10
# 标的不支持 10x 时自动回退，最低接受 5x；再低会因保证金占用过高而跳过。
min_exchange_leverage = 5
risk_per_trade = 0.10
max_positions = 2
max_daily_entries = 6
daily_loss_limit = 0.04
max_gross_multiple = 4.0

stop_pct = 0.05
# 日志显示短促行情常在 +2%～+4% 后反转；达到 +2% 即开始按峰值回撤 1% 保护。
trail_activation_pct = 0.02
trail_pct = 0.01
max_hold_hours = 6
cooldown_hours = 8
# 信号价到实际可成交价最多允许 1.5% 不利偏移；超出则 IOC 限价单不追。
max_entry_slippage_pct = 0.015

min_24h_volume_usd = 5000000.0
min_return_1h = 0.04
max_return_1h = 0.45
min_return_4h = 0.06
max_return_4h = 1.20
min_volume_ratio = 3.0
min_efficiency = 0.35
min_close_location = 0.60

# 先按当前 24h 活跃度选出扫描池，再用 7 天滚动基线做因果判断。
scan_limit = 60
poll_seconds = 60

# 高风险模块默认禁止主网实盘；paper/dry 不受影响。
allow_live = false
