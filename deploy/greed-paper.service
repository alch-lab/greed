[Unit]
Description=greed Binance Demo strategy runtime
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
WorkingDirectory=/opt/greed
ExecStart=/opt/greed/target/release/greed paper --config config/demo.toml
Restart=always
RestartSec=10
EnvironmentFile=-/etc/greed-paper.env

# Binance credentials must belong to Demo. GREED_WEB_PASSWORD in the same file
# enables authenticated operator controls; unauthenticated visitors are read-only.
NoNewPrivileges=true
ProtectSystem=full
ProtectHome=true
PrivateTmp=true
ReadWritePaths=/opt/greed/data
User=greed
Group=greed

[Install]
WantedBy=multi-user.target
